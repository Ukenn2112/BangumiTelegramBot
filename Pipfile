[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aiohttp = ">=3.8.1"
flask = ">=2.1.3"
lxml = ">=4.9.2"
pillow = ">=9.4.0"
pytelegrambotapi = ">=4.10.0"
pyyaml = ">=6.0"
redis = ">=4.3.4"
requests = ">=2.28.1"
waitress = ">=2.1.2"

[dev-packages]

[requires]
python_version = "3.9"
